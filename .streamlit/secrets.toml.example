# Copy this file to .streamlit/secrets.toml for local testing,
# or paste these directly into Streamlit Community Cloud's
# "Secrets" settings when deploying (never commit the real file).
GROQ_API_KEY = "your_api_key_here"
